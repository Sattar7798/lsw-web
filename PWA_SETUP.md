# راهنمای پیکربندی PWA

این وبسایت به عنوان یک Progressive Web App (PWA) پیکربندی شده است.

## فایل‌های مورد نیاز

### 1. ✅ Manifest (تکمیل شده)
- فایل: `public/manifest.json`
- شامل تنظیمات RTL و Farsi
- Icons و shortcuts تعریف شده

### 2. ⚠️ Icons (نیاز به ایجاد)

شما باید دو آیکون ایجاد کنید:

**از لوگو فعلی (`public/logo.jpg`) استفاده کنید:**

```bash
# با استفاده از ابزار آنلاین یا Photoshop:
# 1. باز کردن logo.jpg
# 2. تغییر سایز به 192x192 ← ذخیره به public/icon-192x192.png
# 3. تغییر سایز به 512x512 ← ذخیره به public/icon-512x512.png
```

**یا از ابزار آنلاین استفاده کنید:**
- https://www.simicart.com/manifest-generator.html/
- آپلود logo.jpg
- دانلود آیکون‌های PWA

### 3. 📸 Screenshots (اختیاری)

برای بهبود تجربه نصب:
- `screenshot-wide.png` (1280x720) - دسکتاپ
- `screenshot-mobile.png` (750x1334) - موبایل

## ویژگی‌های فعال

✅ **نصب به صورت App**
- کاربران می‌توانند سایت را به عنوان app نصب کنند
- دکمه "Add to Home Screen" در موبایل

✅ **Offline Experience** (Next.js default)
- Next.js به طور پیش‌فرض static files را cache می‌کند
- برای offline کامل، Service   Worker اضافه کنید

✅ **PWA Shortcuts**
- میانبر سریع به صفحه اصلی
- میانبر سریع به صفحه تماس

## تست PWA

### 1. Chrome DevTools
1. F12
2. Application > Manifest
3. بررسی تمام فیلدها
4. Application > Service Workers

### 2. Lighthouse
1. F12 > Lighthouse
2. انتخاب "Progressive Web App"
3. Run audit
4. هدف: Score > 90

### 3. موبایل
1. باز کردن در Chrome موبایل
2. منو > "Add to Home Screen"
3. تست app نصب شده

## نکات مهم

1. **HTTPS الزامی است** - PWA فقط روی HTTPS کار می‌کند
2. **Icons ضروری هستند** - بدون آیکون، PWA کامل نیست
3. در production، Next.js به طور خودکار استفاده می‌شود

## بررسی PWA

عناصر چک‌لیست PWA:
- ✅ Manifest.json با پیکربندی کامل
- ✅ Metadata در layout
- ⚠️ Icons (باید توسط کاربر ایجاد می‌شوند)
- ✅ HTTPS در production (با Vercel)
- ✅ Responsive design
- ✅ Fast load times

PWA شما آماده است! فقط باید icons را اضافه کنید.
